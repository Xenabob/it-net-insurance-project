@model InsuranceCompany.Models.Clients

@{
    ViewData["Title"] = "Delete";
}

<h2>
    Delete Confirmation
</h2>

<div>
    <p>
        Please confirm you want to delete this client:
        <i>
            @Html.DisplayFor(model => model.FIRST_NAME)&nbsp;@Html.DisplayFor(model => model.LAST_NAME)
        </i>
    </p>
</div>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <input class="btn btn-danger" name="confirmButton" type="submit" value="Delete" />
}